{
  "name": "inklint",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "./node_modules/.bin/tsc -p ./ && ./node_modules/.bin/browserify ./public/lib/index.js -o public/main.js",
    "pages": "npm run build && cp -r public/* pages/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^12.0.1",
    "typescript": "^1.6.2"
  },
  "dependencies": {
    "ikaloghub": "0.0.2"
  }
}
